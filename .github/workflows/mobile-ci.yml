name: Harmonia Mobile CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Use Node 22
        uses: actions/setup-node@v7
        with:
          node-version: 22.13.0
          package-manager-cache: false

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Validate Expo dependencies
        run: npx expo install --check

      - name: Validate catalog sync script
        run: |
          npm run sync:catalog
          node -e "const c=require('./assets/catalog/harmonia-catalog.json'); console.log('[catalog-ci]', c.stats)"

      - name: Lint
        run: npm run lint

      - name: TypeScript
        run: npm run typecheck

      - name: Playback tests
        run: npm test

      - name: Validate Expo config
        run: npx expo config --type public
